package com.aptana.ide.io.file.epl;

import org.eclipse.osgi.util.NLS;

public class OpenWithMenuExternalMessages extends NLS {

    private static final String BUNDLE_NAME = "com.aptana.ide.io.file.epl.menuexternalmessages"; //$NON-NLS-1$

    public static String OpenWithMenuExternal_ERR_ErrorFindingContentType;

    static {
        // initialize resource bundle
        NLS.initializeMessages(BUNDLE_NAME, OpenWithMenuExternalMessages.class);
    }

    private OpenWithMenuExternalMessages() {
    }
}
